/*
Ternary operator - cause there are 3 parts to the condition

ex: Find the greater of the two given numbers
maxNumber = (number1 > number2) ? number1 : number2;

SAME AS

if (number > number2)
    maxNumber = number1;

else
    maxNumber = number2;

*/